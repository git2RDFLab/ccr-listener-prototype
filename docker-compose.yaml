version: '3.1'

services:
  postgresdb:
    image: postgres:15.5
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: example
      POSTGRES_DB: gitrdfdb
    ports:
      - "5432:5432"
